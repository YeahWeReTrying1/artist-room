# Рисованный шрифт для artist

Положи сюда свой вариативный рисованный шрифт:

- `hand-variable.woff2` (предпочтительно)
- или `hand-variable.ttf` / `hand-variable.otf`

Он автоматически подтянется в подписи через класс `.handCaption`.
